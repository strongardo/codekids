# Django Security
SECRET_KEY=your-secret-key-here
DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1

# CSRF (для защиты форм — в локалке можно не указывать, но оставим для совместимости)
CSRF_TRUSTED_ORIGINS=http://localhost

# Localization
LANGUAGE_CODE=ru-ru
TIME_ZONE=Asia/Bishkek

# PostgreSQL (контейнер db)
POSTGRES_DB=codekids_db
POSTGRES_USER=codekids_user
POSTGRES_PASSWORD=codekids_pass

# Django DB settings (используются в settings.py)
DB_NAME=codekids_db
DB_USER=codekids_user
DB_PASSWORD=codekids_pass
DB_HOST=db
DB_PORT=5432

